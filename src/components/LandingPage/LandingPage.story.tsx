import React from 'react';
import { storiesOf } from '@storybook/react-native'
import { Text } from 'react-native';
import LandingPage from './LandingPage'

storiesOf('yeet', module).add('to storybook', () => <LandingPage /> )